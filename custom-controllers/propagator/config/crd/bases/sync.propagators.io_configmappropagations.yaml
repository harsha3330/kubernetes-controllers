---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: configmappropagations.sync.propagators.io
spec:
  group: sync.propagators.io
  names:
    kind: ConfigMapPropagation
    listKind: ConfigMapPropagationList
    plural: configmappropagations
    singular: configmappropagation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigMapPropagation is the Schema for the configmappropagations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ConfigMapPropagation
            properties:
              allowSystemNamespaces:
                default: true
                description: AllowSystem Namespaces determines if propagator needs
                  to target System Namespace
                type: boolean
              createIfMissing:
                default: true
                description: GlobalCreateIfMissing determines whether to create a
                  target Configmap when the configmap is not present
                type: boolean
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy tell what to do about the target configmap when the configmap is deleted
                  - Delete: Deletes the target ConfigMaps
                  - Orphan: Does not delete the target ConfigMaps
                enum:
                - Delete
                - Orphan
                type: string
              namespaceSelector:
                description: |-
                  NamespaceSelector selects namespaces where the target ConfigMap
                  should be propagated.

                  This reuses standard Kubernetes LabelSelector.
                  Example:
                    namespaceSelector:
                      matchLabels:
                        team: backend

                  Use Empty Object to match all namespaces example: namespaceSelector: {}
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              propagationPolicy:
                default: Merge
                description: |-
                  PropagationPolicy determines how the Confimaps should be refreshed:
                  - Overwrite: Keeps the target and source in sync and deletes the extra keys (Absolute Mirror)
                  - Merge: Add the keys without deleting the extra keys
                enum:
                - Merge
                - Overwrite
                type: string
              source:
                description: |-
                  PropagationSource Defines the input for Propagation
                  Input the Configmap's name and namespace
                  If Namespace is not given , default namespace will be taken as input
                properties:
                  name:
                    description: Name of the Configmap
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  namespace:
                    default: default
                    description: Namespace of the configmap
                    type: string
                required:
                - name
                type: object
              syncInterval:
                default: 5m
                description: |-
                  SyncInterval determines how often to sync the target Configmap
                  Only Used when syncmode is periodic
                type: string
              syncMode:
                default: OnChange
                description: |-
                  SyncMode determines how the Confimaps should be refreshed:
                  - CreatedOnce: Creates the Configmap only if it does not exist and does not update it thereafter
                  - Periodic: Synchronizes the Configmap from the external source at regular intervals specified by refreshInterval.
                    No periodic updates occur if refreshInterval is 0.
                  - OnChange: Only synchronizes the Secret when the Configmap's metadata or specification changes
                enum:
                - CreatedOnce
                - Periodic
                - OnChange
                type: string
              targets:
                description: Explicit list of target namespaces/ConfigMaps.
                items:
                  properties:
                    name:
                      description: Name of the target ConfigMap. If not provided,
                        defaults to source name.
                      type: string
                    namespace:
                      description: Namespace where the propagated ConfigMap should
                        be created/updated.
                      minLength: 1
                      type: string
                  required:
                  - namespace
                  type: object
                type: array
            required:
            - createIfMissing
            - source
            type: object
          status:
            description: status defines the observed state of ConfigMapPropagation
            properties:
              checkSum:
                description: |-
                  CheckSum represents the checksum of the desired state (derived from
                  source ConfigMap data + propagation policy). Used to quickly detect if
                  the desired state has changed between reconciliations.
                type: string
              conditions:
                description: |-
                  Conditions follow the standard Kubernetes conditions pattern.
                  Common types:
                  - Ready:    "True" when all targets are synced successfully
                  - Error:    "True" when an unrecoverable issue exists

                  This list uses a map-style structure for efficient updates.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncedAt:
                description: |-
                  LastSyncedAt is the timestamp of the most recent reconciliation attempt
                  (successful or failed). Useful for knowing controller liveness.
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the metadata.generation that the controller
                  has last fully reconciled. Ensures users know the Status reflects
                  the latest Spec.
                format: int64
                type: integer
              targetStatuses:
                description: |-
                  TargetStatuses contains detailed per-target records ONLY for targets that
                  failed, drifted, or were skipped. Healthy ones are omitted to avoid bloating
                  the CR in large clusters.
                items:
                  description: |-
                    TargetStatus represents the sync condition of a single target ConfigMap.
                    Only include entries for failures, drift, or skipped targets to keep the
                    status compact and readable.
                  properties:
                    message:
                      description: |-
                        Message is a human-readable explanation describing the issue or action taken.
                        Typically contains error details or drift description.
                      type: string
                    name:
                      description: Name of the target ConfigMap.
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the target ConfigMap.
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        Reason is a short machine-friendly code providing context for State.
                        Examples: "PermissionDenied", "DriftDetected", "NotFound".
                      type: string
                    state:
                      description: |-
                        State represents the controller's result for this specific target.
                        Common values:
                        - "Synced"   : successfully reconciled
                        - "Failed"   : update or creation error occurred
                        - "Drifted"  : manual modifications detected
                        - "Skipped"  : skipped due to CreateOnce or missing permissions
                      minLength: 1
                      type: string
                  required:
                  - name
                  - namespace
                  - state
                  type: object
                type: array
              targetsSummary:
                description: |-
                  TargetsSummary gives a compressed overview of how many targets succeeded
                  or failed during reconciliation.
                properties:
                  failed:
                    description: Number of targets that failed due to drift, permissions,
                      or update errors.
                    format: int32
                    type: integer
                  synced:
                    description: Number of targets that reached the desired state
                      successfully.
                    format: int32
                    type: integer
                  total:
                    description: Total number of target namespaces evaluated for this
                      propagation.
                    format: int32
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
